Changes I want:

- Settings page:
How about the system theme, use Material if the applicatin supports it? light and dark theme is hard coded like we have, but the system theme specifically should use Material You colors if the environment/android version has them, if colors are too contrasted or no material data is present simply default to dark/light automatic recognition

- Camera Page:
Application should only scan the qr if it's in the camera frame, do not scan if it's outside the frame, also for this change please cover up the surroundings of the frame so that user's can't see there, have the dimmed screen take up the colors from surroundings, light if light theme, system if system theme and black if black themed
